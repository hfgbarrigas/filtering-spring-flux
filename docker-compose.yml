version: '3'
services:
  service:
    build: .
    ports:
     - "8080:8080"
    links:
      - mongo:mongo
    depends_on:
      - mongo
    environment:
      - ENVIRONMENT=qa
  mongo:
    image: "mongo:latest"
    container_name: mongo